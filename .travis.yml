sudo: false
language: python
services:
  - redis
python:
  - "3.6"
  - "3.7"
  - "3.8"
install:
  - pip install -r requirements.txt
  - pip install -e .
  - pip install flake8 flake8-isort
script:
  - pytest
  - flake8 .
